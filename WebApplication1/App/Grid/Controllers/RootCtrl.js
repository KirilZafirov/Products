gridModule.controller("rootCtrl",
    function ($scope, gridService, $http, $q, viewModelHelper) {

        $scope.viewModelHelper = viewModelHelper;
        $scope.gridService = gridService;

        var initialize = function () {
        };

        initialize();

    });